<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pedidos.android.pacoportela.com.pedidos">

    <application
        android:allowBackup="true"
        android:icon="@drawable/elco"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ActividadPrincipal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".ActividadEditar"
            android:exported="false"
            android:label="Modificar ó borrar yogur">
            <intent-filter>
                <action android:name="c" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ActividadAyuda"
            android:exported="false"
            android:label="Ayuda">
        </activity>

    </application>

</manifest>